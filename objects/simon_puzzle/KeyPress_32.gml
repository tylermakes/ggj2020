/// @description Insert description here
// You can write your code in this editor
if (!complete) {
	switch(selection) {
		case 0: // left long
			//offsetArm += irandom_range(-LONG_MAX, -LONG_MIN)
		break;
		case 1: // left short
			//offsetArm += irandom_range(-SHORT_MAX, -SHORT_MIN)
		break;
		case 2: // right short
			//offsetArm += irandom_range(SHORT_MIN, SHORT_MAX)
		break;
	}
}